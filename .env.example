# Plugin Manager Configuration Examples
# Copy these to your application's .env file and modify as needed

# Plugin directory (relative to app base path)
#PLUGIN_DIRECTORY=app/Plugins

# Plugin namespace
#PLUGIN_NAMESPACE=App\\Plugins

# Plugin class suffix (e.g., "Plugin" means TestPlugin for Test plugin)
#PLUGIN_CLASS_SUFFIX=Plugin

# View namespace prefix (e.g., "plugin" means plugin:test::view)
#PLUGIN_VIEW_NAMESPACE_PREFIX=plugin

# Storage directory for plugin cache files
#PLUGIN_STORAGE_DIRECTORY=plugins

# Enable/disable plugin class mapping cache
#PLUGIN_CACHE=true

# Error handling mode: exception, log, or silent
#PLUGIN_ERROR_MODE=exception

# Auto-create plugin directory if it doesn't exist
#PLUGIN_AUTO_CREATE_DIRECTORY=true